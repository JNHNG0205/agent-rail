const { expect } = require("chai");
const { ethers } = require("hardhat");

describe("Contract Scaffold Test", function () {
  it("Should compile interface and mock contracts cleanly", async function () {
    expect(true).to.equal(true);
  });
});
