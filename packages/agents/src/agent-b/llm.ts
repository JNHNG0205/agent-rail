/// Calls the LLM to actually perform the hired task. Member 4.
/// Uses LLM_API_KEY / LLM_MODEL from the environment.
export async function runTask(prompt: string): Promise<string> {
  // TODO(M4): call the LLM API (model from process.env.LLM_MODEL) and return
  //           the deliverable text. Keep the request minimal for the demo.
  void prompt;
  throw new Error("TODO(M4): runTask()");
}
